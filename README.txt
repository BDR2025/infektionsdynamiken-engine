In den nachfolgend beschriebenen Ebenen wird die Explore Engine V1 im aktuellen Stand dargestellt. Sie entspricht der live verfügbaren Version unter und ist dort unmittelbar erreichbar.

Die Darstellung folgt der Vier-Schichten-Architektur mit Basis, Input, Darstellung und Support. Alle Module erscheinen so, wie sie produktiv vorliegen. Zusätzliche Berechnungen finden nicht statt. Ziel ist eine klare und nachvollziehbare Abbildung der aktuellen Funktionalität, leicht zugänglich für Lernende und übersichtlich für Entwicklerinnen und Entwickler.
